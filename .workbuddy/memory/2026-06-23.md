# 2026-06-23 工作日志

## 命中率动态计算系统 (14:53)
- 预测比分命中率：主预测 OR 备选比分命中实际比分 → 命中
- 推演比分命中率：Dixon-Coles 前两名精准比分任一命中 → 命中
- 统计起算日期：北京时间 2026-06-23（之前的场次不计入）
- 新建 `calc_hit_rates.py`：解析 review-*.html 取实际比分 → 交叉 match_data.json + simulation_scores.json
- `build_site.py` 集成：每次构建自动运行 calc_hit_rates() → 输出 hit_rates.json
- `index.html` 动态加载 hit_rates.json → renderDataGrid() 实时显示
- 当前状态：0/0（尚无 6/23+ 的 review 报告）— 复盘生成后自动更新
- CloudStudio 部署 + GitHub 推送 (commit 508ab27)

## 新增备选比分显示 (14:24)
- 预测比分下方新增灰色小字备选比分（推演第二比分）
- 中心单元格改为 flex 竖排：主比分 24px 黑色 + 备选 12px 灰色
- 仅当 simulation_scores.json 有第二比分时显示
- CloudStudio 部署 + GitHub 推送 (commit 2e5aba5)

## 卡片推演比分替换让球盘 (14:12)
- Row 4 "参考让球盘" → "推演比分"
- 数据源：解析 Dixon-Coles MD 报告的"推荐精准比分"前两名
- 新建 `build_simulation_data.py`：自动解析 Obsidian dixon-coles-monte-carlo/ 下所有 MD → `simulation_scores.json`
- `index.html`：加载 simulation_scores.json，按 "teamA vs teamB" 键查表
- 当前4场匹配：阿根廷2-1/2-0、法国3-0/4-0、挪威3-1/3-2、约旦1-2/0-2
- Row 4 两列：推演比分(左) + 参考赔率(右)
- CloudStudio 部署 + GitHub 推送 (commit 7ab8350)

## 新增推演底栏标签 (13:49)
- 底部导航新增"推演"标签页，位于"预测"和"玄学"之间
- 内容来源：`D:/我的坚果云/OB笔记/自媒体/fwc2026/content/dixon-coles-monte-carlo/`
- 新增 `articles/simulation/` 目录存放 Dixon-Coles 蒙特卡洛精算预测 HTML
- 新增 `md2html.py`：手动转换脚本（含品牌蓝配色方案）
- 修改 `build_site.py`：自动从 Obsidian 目录转换 simulation MD → HTML
- 修改 `index.html`：新增 `page-simulation` 区域、nav button（齿轮图标）、`renderSimulation()` 函数、hash routing
- CSS 新增 `.article-icon.simulation { background: #fff7ed; }`（暖橙色底）
- 导航顺序：首页 → 预测 → **推演** → 玄学 → 专题
- CloudStudio 部署 + GitHub 推送 (commit 32c76cb)

## 专题页链接修复 (08:15)
- 专题页10篇文章中7篇链接文件名与实际文件不匹配，全部404
- 对照 `articles/research/` 目录实际文件，修正 index.html 中的 articles 数组
- 实际文件：3篇教练分析（文件名正确）+ 大小球分析 + 大小球博彩分析 + 大比分预测 + 大比分历史分析 + 进球预测 + 前三场进球历史 + 小组赛第一轮分析
- 去掉不存在的"冷门预测""球队进攻/防守风格分类"等虚假条目
- CloudStudio 部署 + GitHub 推送 (commit aa6a419)

## 自动化任务更新 (13:45)

### 写文稿 (automation-1782113868431)
- 新增「零、数据准确性铁律」章节（5条）：北京时间、FIFA交叉核对、文件命名日期、时区精确、小组归属
- 赛程日期核对步骤强化：强制用 FIFA 官网交叉核对 schedule.json
- 5.1/5.2 明确文件名日期必须为北京时间
- 5.3 简报新增「文件命名日期核验」输出

### 校对文稿 (automation-1782113912520)
- **新增第3步「内容质量提升与重写」**：用户要求校对同时重写提升内容质量
- 铁律从2条扩展为4条（新增：schedule.json非绝对权威、文件命名日期不可协商）
- 内容重写5个维度：可读性表达、论据强度、数据呈现、教练视角检查、最终结论质量
- 原则变更：「校对不是重写」→「校对+重写同时进行，交付成品级报告」
- 新增球员数据核对（年龄、位置、俱乐部归属）
- 校对报告模板新增「内容重写记录」章节
- 严禁改动预测结果数值（胜率%、比分、xG区间保持原样）
